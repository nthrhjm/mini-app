.wrapper
  .header
    = render partial: "header"
  .body.transparent
    - @posts.each do |post|
      = render partial: "post", locals: { post: post}
    .row
      .col.s12.m6
        .pagination
          = paginate(@posts)
